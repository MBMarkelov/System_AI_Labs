<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AvaloniaUI.ViewModels.AuntificationModel;assembly=AvaloniaUI"
        x:DataType="vm:RegistrationViewModel"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="AvaloniaUI.Window1"
        Title="Window1">
  Welcome to Avalonia!
=======
        xmlns:vm="clr-namespace:AvaloniaUI.ViewModels.AuntificationModel;assembly=AvaloniaUI"
        x:DataType="vm:RegistrationViewModel"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="550"
        x:Class="AvaloniaUI.RegistrationWindow"
        Title="Регистрация пользователя" Width="400" Height="500">
        <ScrollViewer>
            <StackPanel Margin="20" Spacing="8">
                <TextBlock Text="Registration user" FontSize="16" HorizontalAlignment="Center"/>
                <TextBox Watermark="Full Name" Text="{Binding User.FullName}"/>
                <TextBox Watermark="Email" Text="{Binding User.Email}"/>
                <TextBox Watermark="Phone Number" Text="{Binding User.PhoneNumber}"/>
                <TextBox Watermark="Telegram" Text="{Binding User.Telegram}"/>
                <TextBox Watermark="Build" Text="{Binding User.Address.House}"/>
                <TextBox Watermark="Street" Text="{Binding User.Address.Street}"/>
                <TextBox Watermark="City" Text="{Binding User.Address.City}"/>
                <TextBox Watermark="District" Text="{Binding User.Address.District}"/>
                <TextBox Watermark="Region" Text="{Binding User.Address.Region}"/>
                <TextBox Watermark="Postal Code" Text="{Binding User.Address.PostalIndex}"/>
                <TextBox Watermark="Country" Text="{Binding User.Address.Country}"/>
                <Button Content="Sing up" Command="{Binding RegisterCommand}"/>
            </StackPanel>
        </ScrollViewer>
>>>>>>> Stashed changes
</Window>
